[pytest]
# Configuração única da bateria de testes do GSC Monitor.
# Rode toda a suíte com:  py -m pytest
# (ou um arquivo só:       py -m pytest tests/test_classifier.py -v)
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
