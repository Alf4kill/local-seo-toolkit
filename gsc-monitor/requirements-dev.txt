# Dependências apenas para desenvolvimento e testes.
# Instale com: pip install -r requirements-dev.txt
#
# pytest é o runner único da suíte (config em pytest.ini); ruff faz lint + format
# (config em ../ruff.toml). As deps de runtime ficam em requirements.txt.

pytest>=9.0.3
pytest-cov>=7.1.0
ruff==0.15.17
